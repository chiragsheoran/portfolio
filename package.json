{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp *.jpg ./dist/ && cp *.jpeg ./dist/ && cp -r models ./dist/models",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.8.0"
  },
  "dependencies": {
    "three": "^0.138.3"
  }
}
